<!DOCTYPE html>
<html lang="vi">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Chi Tiết Nhân Viên: <%= employee.HoTen %></title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />

    <style>
      /* CSS tùy chỉnh bổ sung */
      .profile-img-container {
        width: 150px;
        height: 150px;
        overflow: hidden;
        border-radius: 50%;
        border: 4px solid var(--bs-primary); /* Sử dụng màu primary của Bootstrap */
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
      }
      .profile-img {
        width: 100%;
        height: 100%;
        object-fit: cover;
      }
      .detail-label {
        font-weight: 500;
        color: #6c757d; /* Màu chữ xám nhạt */
      }
      .detail-value {
        font-weight: 600;
        color: #212529; /* Màu chữ đậm */
      }
    </style>
  </head>
  <body>
    <div class="container my-5">
      <div class="card shadow-lg border-0">
        <div class="card-header bg-primary text-white text-center py-3">
          <h2 class="mb-0">Thông Tin Chi Tiết Nhân Viên</h2>
        </div>
        <div class="card-body p-4 p-md-5">
          <div class="row mb-4 align-items-center">
            <div class="col-12 col-md-3 text-center mb-4 mb-md-0">
              <div class="profile-img-container mx-auto">
                <img
                  src="<%= employee.ImgURL || '/images/default-avatar.png' %>"
                  alt="Ảnh đại diện của <%= employee.HoTen %>"
                  class="profile-img"
                />
              </div>
            </div>

            <div class="col-12 col-md-9">
              <h3 class="display-6 fw-bold text-dark"><%= employee.HoTen %></h3>
              <p class="lead text-muted">
                Mã NV:
                <span class="badge bg-secondary"><%= employee.MaNV %></span>
              </p>
              <p class="text-success"><%= employee.TrangThai %></p>
            </div>
          </div>

          <hr class="my-4" />

          <h4 class="mb-3 text-primary">Thông Tin Cá Nhân & Công Việc</h4>
          <div class="row g-3">
            <div class="col-md-6">
              <p class="mb-0 detail-label">Ngày Sinh:</p>
              <span class="detail-value">
                <%= employee.NgaySinh ? new
                Date(employee.NgaySinh).toLocaleDateString('vi-VN') : 'N/A' %>
              </span>
            </div>
            <div class="col-md-6">
              <p class="mb-0 detail-label">Giới Tính:</p>
              <span class="detail-value"><%= employee.GioiTinh %></span>
            </div>

            <div class="col-md-6">
              <p class="mb-0 detail-label">Phòng Ban:</p>
              <span class="detail-value"><%= employee.PhongBan %></span>
            </div>
            <div class="col-md-6">
              <p class="mb-0 detail-label">Ngày Vào Làm:</p>
              <span class="detail-value">
                <%= employee.NgayVaoLam ? new
                Date(employee.NgayVaoLam).toLocaleDateString('vi-VN') : 'N/A' %>
              </span>
            </div>

            <div class="col-md-6">
              <p class="mb-0 detail-label">Email:</p>
              <span class="detail-value"><%= employee.Email %></span>
            </div>
            <div class="col-md-6">
              <p class="mb-0 detail-label">Số Điện Thoại (SĐT):</p>
              <span class="detail-value"><%= employee.SDT %></span>
            </div>

            <div class="col-12">
              <p class="mb-0 detail-label">Địa Chỉ:</p>
              <span class="detail-value"><%= employee.DiaChi %></span>
            </div>

            <% if (employee.Password) { %>
            <div class="col-12">
              <p class="mb-0 detail-label text-danger">
                Mật Khẩu (Để kiểm tra):
              </p>
              <span class="detail-value text-danger fst-italic">
                <%= employee.Password.substring(0, 15) %>... (Đã mã hóa)
              </span>
            </div>
            <% } %>
          </div>
        </div>
        <div class="card-footer text-end text-muted">
          <small
            >Cập nhật lần cuối: <%= new Date().toLocaleDateString('vi-VN')
            %></small
          >
        </div>
      </div>

      <div class="mt-4 text-center">
        <a href="/employees" class="btn btn-outline-primary"
          >&larr; Quay lại danh sách</a
        >
      </div>
    </div>

    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
      crossorigin="anonymous"
    ></script>
  </body>
</html>
